package com.example.authhub.send_events

object ClientAuthHub {

    // função para incializar o modulo(fake) criando a instancia do eventManager dentro da classe
    // que vai notificar os modulos de acordo com o evento.
    fun init() {
        ManagerRecommendation
    }
}